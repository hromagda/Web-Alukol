<div class="container" style="margin-top: 100px; max-width: 800px;">
    <div class="card shadow">
        <div class="card-body">
            <?php if (isset($title)): ?>
                <h1 class="card-title text-center mb-4"><?= htmlspecialchars($title) ?></h1>
            <?php endif; ?>

            <?php if (isset($content)): ?>
                <?= $content ?>
            <?php endif; ?>
        </div>
    </div>
</div>
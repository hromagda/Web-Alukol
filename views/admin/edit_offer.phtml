<?php
// Zachytíme HTML formulář a další obsah jako $content
ob_start();
?>

    <p>Jste přihlášen jako <strong><?= htmlspecialchars($username) ?></strong>.</p>

<?php if ($success): ?>
    <div class="alert alert-success"><?= $success ?></div>
<?php endif; ?>
<?php if ($error): ?>
    <div class="alert alert-danger"><?= $error ?></div>
<?php endif; ?>

<form method="post" class="mb-4">
    <?= csrf_field() ?>
    <div class="mb-3">
        <label for="content" class="form-label">Text akční nabídky:</label>
        <textarea name="content" id="content" class="form-control" rows="5" required><?= htmlspecialchars($offer['content'] ?? '') ?></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Uložit změny</button>
</form>

<?php include __DIR__ . '/../partials/back_button.phtml'; ?>

<?php
$content = ob_get_clean();
$title = 'Editace akční nabídky';
include __DIR__ . '/../partials/admin_card.phtml';
?>


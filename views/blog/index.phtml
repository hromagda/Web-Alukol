<div class="container mt-5 pt-5 blog-content">
    <h1 class="mb-4">Blog</h1>


    <?php foreach ($articles as $article): ?>
    <div class="mb-5 border-bottom pb-3">
        <h2><?= htmlspecialchars($article['title']) ?></h2>
        <p class="text-muted"><?= date('j. n. Y', strtotime($article['created_at'])) ?></p>

        <?php if (!empty($article['image'])): ?>
            <img src="/<?= htmlspecialchars($article['image']) ?>" alt="Náhled" class="img-fluid mb-3" style="max-width: 100%; height: auto;">
        <?php endif; ?>

        <p><?=  mb_substr(strip_tags($article['content']), 0, 300) ?>...</p>
        <a href="/blog/detail/<?= urlencode($article['slug']) ?>" class="btn btn-outline-primary">
            Číst dále &rarr;
        </a>
    </div>
<?php endforeach; ?>